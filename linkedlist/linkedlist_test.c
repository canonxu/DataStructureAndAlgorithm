#include <stdio.h>
#include "linkedlist.h"

int main()
{
    printf("Start!\n");
    CreatEmptyList();

/*
    typedef struct Stu stu;

    struct Stu
    {
        int num;
        int score;
    };

    stu canonxu;
    canonxu.num = 1140349174;
    canonxu.score = 95;
    printf("%d",canonxu.num);
*/
    return 0;
}
